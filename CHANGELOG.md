# CHANGELOG

## GestureClip v0.6.25 Beta (UI fix)

### 设置显示与深色 / 热键

- 修复下拉框显示 `GesturePresetOption { ... }` 类型名：选项实现可读 `ToString`
- 热键框改为**点击录制**（Esc 取消，Backspace 恢复默认），字体与对比度修正
- 动作绑定卡片加大字号与内边距，硬编码浅色背景改为主题资源（深色可读）
- 深色色板重做：高对比 slate + 亮蓝强调，正文/次要文字更清晰
- 设置窗口记住位置/大小/最大化状态

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.25 Beta (engineering follow-up)

### 安装器与签名流水线

- 恢复 **Setup 安装包**：`scripts/build-setup.ps1` + `scripts/install/Setup.cmd` / `install.ps1`
- 默认装到 `%LOCALAPPDATA%\Programs\GestureClip`，开始菜单快捷方式，可卸载；**不碰**用户数据目录
- 应用内更新优先 Setup.exe / Setup zip，没有时回退 portable zip 覆盖
- 新增 `scripts/sign-release.ps1`：配置证书则签 exe/dll，未配置则跳过
- `publish-win-x64.ps1` 发布后可选调用签名

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.25 Beta

### 设置界面高保真重设计

- **macOS 风格三栏布局**：左侧分组导航（常用 / 系统 / 其他）、中央全宽设置、可折叠右侧状态栏。
- **浅色玻璃色板**：snow / cloud / slate / sky-blue，半透明卡片与细分割线，减少框套框。
- **总览与剪贴板页扁平化**：分组标题 + 列表容器；智能粘贴轻量强调区；危险操作用文字按钮。
- **手势 / 动作绑定页对齐**：去掉重 GroupBox 外壳，分区标题与列表容器统一节奏。
- **可用全局搜索**：匹配设置项并跳转到对应页面，滚动到具体分组；支持 Enter / Esc。
- **右侧栏可折叠**：中间内容自适应加宽；折叠按钮切换。
- **窗口可缩放**：无边框窗口支持拖边调整；缩放时临时关闭阴影降低卡顿。
- **开关与下拉修复**：macOS 风格开关；动作/边缘 ComboBox 改 SelectedItem，避免选不中。
- **滚动条贴边**：细滚动条（约 6px），更贴近内容区边缘。

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.24 Beta

### 极简高级 UI

- **更克制的色板**：近白/炭灰表面、细发丝描边、低饱和蓝强调色；明暗主题统一气质。
- **更轻的阴影与圆角**：卡片/面板阴影变浅，圆角更收敛（约 10–18），减少“厚重毛玻璃”感。
- **控件更干净**：按钮高度与侧栏 Tab 间距收紧；Tab 选中改为纯色块而非重渐变。
- **窗口壳层统一**：设置 / 剪贴板历史 / 快捷动作外壳阴影与边框对齐主题 token。
- **边缘增强卡片降噪**：背景改为三级填充，避免抢主内容。

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.23 Beta

### 人性化设置

- **更换动作直达编辑区**：动作绑定卡片点「更换动作」会选中该手势并滚到下方修改区，不用自己翻页找。
- **手势页露出边缘增强**：屏幕边缘 / 四角触发从高级折叠里抬到显眼卡片，一键开关 + 浏览器预设 + 完整设置入口。
- **高级设置可发现**：标题改为「高级设置 · 触发键 / 边缘详细项」，并有「展开高级设置」按钮。
- **首页功能地图可点**：智能粘贴 / 手势 / 动作绑定 / 剪贴板 / 隐私卡片点击跳转对应设置页。
- **新人引导可跳转**：欢迎页三张卡片分别打开剪贴板、动作绑定、工位小熊设置，不再只进空白首页。

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.22 Beta

### UI 美化

- **主题色板升级**：明/暗背景渐变、卡片与侧栏色值主题化，切换暗色模式更统一。
- **控件质感**：按钮圆角 12、分段筛选选中态、列表悬停/选中、输入框焦点光晕更干净。
- **剪贴板历史**：面板阴影与圆角、条目卡片抬升、加载条/快捷键浮层边框走主题色。
- **设置与快捷动作**：外壳阴影与卡片背景统一到 `BrushCardElevated`。
- **手势 HUD**：轨迹更清晰，HUD 面板阴影与圆角微调。

### 稳定性（含 v0.6.21）

- **手势粘贴目标窗口**：下滑/智能粘贴注入时带上按下右键时的前台 HWND。
- **粘贴失败人话提示**：管理员窗口等场景日志说明。
- **历史再复制统一纯文本**：去掉 HTML 标签，网页/ChatGPT 再贴更完整。
- **剪贴板读取增强**：Unicode → 通用文本 → CF_HTML→纯文本。
- **防误触默认更保守**：默认触发阈值 20→28、段阈值 16→22（已自定义不受影响）。

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.20 Beta

### 细节（剪贴板更爱用）

- **本会话已复制**：左侧色条变绿 +「已复制」标签；多选可区分进度。
- 底栏摘要：`共 N · 已选 M · 已复制 K`；标题区提示绿条含义。
- 批量复制：混选文字+图片 / 多图时给出可执行的友好提示。
- 智能粘贴说明：只净化剪贴板，注入与关闭时相同的 Ctrl+V。
- HELP / 色条图例 / KNOWN_ISSUES 同步到当前版本。

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.19 Beta

### 修复（智能粘贴开启后无法粘贴）

- **根因**：智能粘贴开启后走「改写剪贴板 + 独立注入并 early return」，与关闭时可靠的 `Ctrl+V` 分叉。
- **统一路径**：智能粘贴只做尽力改写剪贴板（内容不变则跳过）；**始终**再走键盘 `Ctrl+V`。
- 焦点：目标已是前台时不再 `SetForegroundWindow`，避免抢走输入框光标。
- 注入：SendInput → keybd_event → WM_PASTE。

### 修复（下滑手势弹出右键菜单）

- 粘贴注入**不再发送合成鼠标键抬起**（尤其 RIGHTUP，会进应用变成右键菜单）。
- 手势完成后短时吞掉残留的右键抬起（Idle 态），不挡下一次手势。

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.18 Beta

### 修复（下滑手势粘贴跨机）

- x64 `INPUT` 联合体、粘贴前松鼠标/修饰键、HUD 先藏再注入、手势起点记 HWND。

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.17 Beta

### 修复

- 粘贴修饰键污染；历史面板先隐藏再粘贴；无文本回退；Setup 安装器移除。

## GestureClip v0.6.16 Beta

### 修复

- 粘贴修饰键污染初版修复；Setup 安装器下线，保留便携 zip。

### 分发说明

- 使用 **便携 zip** 分发与覆盖更新。
- 应用内更新下载 `*-win-x64.zip` 覆盖程序目录；用户数据在 `%LOCALAPPDATA%\GestureClip`。

### UI 视觉升级（D1–D3）

- **D1 剪贴板**：图标顶栏、分段筛选、类型色条卡片、相对时间、空状态插画。
- **D2 设置**：大卡片分组、字号节奏（22/17/13/11）、**深色模式**开关。
- **D3**：快捷动作 Spotlight 化；手势绑定迷你轨迹 ↑↓←→；工位小熊阶段 Emoji IP。

### 粘贴力爆款包

- **S1** 全局 `Ctrl+Shift+V` 纯文本粘贴（可改键）。
- **S2** 快捷动作：纯文本 / HTML→文本 / 轻量 Markdown / **链接净化**（去 utm/spm 等）。
- **A1** 智能粘贴扩展：普通「粘贴/粘贴并回车」也按前台应用选策略（聊天/IDE 默认纯文本）。
- **A5** 密码框、密码管理器、登录/密码窗口默认不写入剪贴板历史。

### 更新网络

- 检查更新 / 一键覆盖下载改为多路径：系统代理 → 直连 → 公共镜像加速。
- 单次请求缩短超时，避免卡死在失效的本机代理上；成功路径会优先复用。
- 失败文案说明已尝试的路径，并引导手动打开 Release 下载。

### 验证

- `dotnet test ./GestureClip.sln`

## GestureClip v0.6.15 Beta

### 剪贴板人性化快捷

- 一键回顶部（按钮 / `Home`）；`End` 到底部；打开面板默认选中最新。
- 「重置」清空搜索，必要时恢复「全部」筛选并回顶。
- `Ctrl+Enter` 粘贴后保持面板打开；`Enter` 仍为粘贴并关闭。
- 删除后自动选中下一条；支持 `Ctrl+Z` /「撤销删除」恢复最近一次删除。
- `Ctrl+Shift+C` 复制为纯文本（压多余空行）；右键与详情区入口。
- `?` / `F1` 快捷键速查浮层；底栏动态提示。

### 验证

- `dotnet test ./GestureClip.sln`
- `scripts/publish-win-x64.ps1`

## GestureClip v0.6.14 Beta

### 重点更新

- **牛马助手 MVP**：`Ctrl + Shift + Q` 快捷动作面板；本地文本处理（去空格/大小写/JSON/URL 等）；可预览、复制、粘贴。
- 手势可绑定助手动作；设置页可改快捷动作热键。
- **左键增强动作**可在「动作绑定」页增删改，支持恢复默认。
- **工位小熊**：Hub 分区折叠；30 秒月薪/工时引导；休息提醒日/周上限与连续工作阈值；文案按阶段轮换。
- 托盘：今日已赚摘要、下班冲刺开关、本周总结、分享卡片后打开文件夹。
- 分享卡片 4 套样式；本地周报/月报；快捷动作可加 XP；HUD 阶段彩蛋可关。
- **UI**：Apple 风侧栏/交通灯；主按钮白字对比度修复；红关/黄放大/绿缩小。
- **更新**：检查更新显示 Release 说明；GitHub API 请求头与代理失败直连重试；关于页内置 CHANGELOG。

### 验证

- `dotnet test ./GestureClip.sln`
- 建议：`scripts/publish-win-x64.ps1` 后发 GitHub Release

## GestureClip v0.6.13 Beta

### 重点更新

- 修复从 ChatGPT / 浏览器复制富文本内容后，剪贴板历史再次复制可能拿不到完整正文的问题。
- 剪贴板捕获在同时存在文本、HTML、图片快照时优先保存非空 Unicode 文本，避免把网页回复误判成图片。
- 从搜索结果、列表项、双击、复制按钮和粘贴动作复制时，统一走完整正文加载路径，避免只写入预览文本。
- 动作绑定页升级为卡片式展示，显示手势方向、中文名称、当前动作、小白说明、就近“更换动作 / 删除”按钮。
- 新增“推荐给新手的 3 个手势”，可一键补齐缺失推荐配置，不覆盖已有自定义绑定。
- 设置页增加统一入口说明卡、统一按钮宽度、表单行距和危险操作区样式，小白更容易判断每页用途和风险。

### 验证

- `git diff --check`
- `dotnet test ./GestureClip.sln --no-restore`
- `scripts/publish-win-x64.ps1`

## GestureClip v0.6.12 Beta

### 重点更新

- 优化手势设置页：常用手势和高级手势列表支持就近删除，不再只能到动作编辑器里删除。
- 删除手势前新增确认弹窗，避免误触导致自定义手势丢失。
- 手势列表选中项新增“当前选中”提示，并继续沿用浅色自定义选中态，减少系统默认黑块高亮问题。
- 新增 `.editorconfig` 和 `.gitattributes`，降低后续编辑时出现行尾空格、换行污染和二进制文件误识别的风险。

### 验证

- `git diff --check`
- `dotnet test ./GestureClip.sln`
- `scripts/publish-win-x64.ps1`

## GestureClip v0.6.7 Beta

### 修复

- 修复剪贴板历史双击复制长文本时可能明显卡顿、无法复制的问题。
- 文本写入系统剪贴板改为原生 Unicode 写入路径，长文复制更快、更稳定。
- 增加长文本复制回归测试，确保长文不会被截断，也不会等待后台使用次数写库。

### 验证

- `dotnet test ./GestureClip.sln`
- `scripts/publish-win-x64.ps1`

## GestureClip v0.6.6 Beta

### 修复

- 修复剪贴板历史双击复制后窗口偶尔不消失的问题。
- 修复双击复制后马上粘贴时可能卡顿、需要多次粘贴才生效的问题。
- 双击复制现在按鼠标所在记录执行并关闭面板；复制完成后会短暂等待系统剪贴板稳定，并释放覆盖窗口焦点。

### 验证

- `dotnet test ./GestureClip.sln`
- `scripts/publish-win-x64.ps1`

## GestureClip v0.6.5 Beta

### 重点更新

- 手势设置页新增“二次确认添加”：新建自定义手势时，第一次点击只进入确认态，第二次点击才保存，避免误触。
- 修改手势码、重新画手势或更换动作后，会自动取消之前的确认态。
- 手势列表和动作下拉框的选中态改为浅蓝高亮，不再出现突兀黑块。
- 剪贴板历史支持双击记录后自动复制到系统剪贴板并关闭面板，适合“找回刚才复制过的内容”场景。
- 剪贴板历史顶部说明更明确：双击是复制并关闭，Enter/粘贴按钮是粘贴并关闭，复制按钮只复制不关闭。
- 复制成功提示改为“文字/图片已复制到系统剪贴板”，小白更容易确认结果。
- 删除剪贴板历史记录前新增确认弹窗，说明只会删除本机历史，不影响当前系统剪贴板内容。
- 修复手势执行“粘贴 / 粘贴并回车”时只按下 Ctrl+V、但工位统计没有增加粘贴次数的问题。
- 剪贴板历史列表选中态也改为浅蓝高亮，整体视觉更一致。

### 验证

- dotnet test 通过，472 passed。

## GestureClip v0.6.4 Beta

### 修复

- 修复检查更新在系统代理指向 `127.0.0.1:7890` 但代理未启动时直接失败的问题。
- 检查更新和下载更新在系统代理失败后会自动直连重试。
- 优化检查更新失败文案，不再把 `127.0.0.1:7890` 和底层连接拒绝错误直接展示给用户。

### 验证

- dotnet build 通过。
- dotnet test 通过，472 passed。

## GestureClip v0.6.3 Beta

### 重点更新

- 新增“检查更新”：设置页和托盘菜单可比较当前版本与 GitHub Latest Release。
- 检查更新会提示“当前已是最新版本”或“发现新版本”，发现新版本后可直接进入一键覆盖安装。
- 自动更新服务拆分为检查与安装两层，后续更容易扩展自动检查、忽略版本和更新说明展示。

### 验证

- dotnet build 通过。
- dotnet test 通过，471 passed。

## GestureClip v0.6.2 Beta

### 重点更新

- 新增真正的一键覆盖更新：设置页和托盘菜单可自动下载 GitHub Latest Release 的 Windows x64 zip。
- 更新器会解压到临时目录，退出当前程序后复制新版文件覆盖旧版并自动重启 GestureClip。
- 覆盖安装时保留本地数据库、剪贴板历史、设置和日志目录，降低小白手动更新成本。
- 自动更新失败时会打开 GitHub 最新 Release 页面，仍可手动下载 zip 覆盖安装。

### 验证

- dotnet build 通过。
- dotnet test 通过，467 passed。

## GestureClip v0.6.1 Beta

### 重点更新

- 剪贴板历史重复复制体验优化：之前复制过的内容再次复制时会刷新到列表顶部，不再需要往下翻找。
- 剪贴板历史面板改为更高、更紧凑的浏览布局，一次能看到更多记录。
- 数字快捷选择从 `1-9` 扩展为 `1-0`，支持快速选择前 10 条记录。
- 新增首次启动引导：用三步说明剪贴板历史、鼠标手势和工位小熊。
- Release 打包改为更干净的资产列表：只生成 Windows x64 zip 和 `SHA256SUMS.txt`，不再额外生成内容相同的 update 包。

### 修复

- 修复重复复制旧内容后仍停留在原位置的问题。
- 保持置顶记录优先，同时普通记录按最近活跃时间排序。

### 验证

- dotnet build 通过。
- dotnet test 通过，466 passed。

## GestureClip v0.6.0 Beta

### 重点更新

- 工位小熊升级为办公状态 Hub：顶部实时状态卡、今日收益与效率、摸鱼与休息、下班冲刺、今日报告。
- 新增手动摸鱼模式，记录本次/今日摸鱼时长与娱乐估算价值。
- 新增下班后自动报告，每天最多弹出一次，支持今天不再显示。
- 新增 PNG 分享卡，默认保存桌面，不上传、不自动分享、不包含剪贴板内容。
- HUD 联动工位状态，支持阶段变色和高频“复制 +5”合并提示。
- 托盘菜单新增工位小熊子菜单：Hub、摸鱼、报告、分享卡、休息提醒。
- 剪贴板历史新增“常显”按钮：默认失焦自动隐藏，选择常显后才置顶常驻。
- 设置页同步新增自动报告、摸鱼、下班冲刺、分享卡、HUD 状态文案、HUD 阶段变色等开关。
- 工位小熊 Hub 数据库迁移改为可重复检查，增强旧库恢复能力。

### 隐私与性能

- 报告和分享卡不包含剪贴板正文、图片内容、浏览器内容或敏感路径。
- 工位小熊统计全部本地保存，不上传。
- HUD 使用短缓存，托盘菜单避免同步阻塞，分享卡后台生成。

### 验证

- dotnet restore 通过。
- dotnet build 通过，0 警告 0 错误。
- dotnet test 通过，459 passed。

## GestureClip v0.5.1 Beta

### 重点更新

- 升级发布版本为 `0.5.1-beta`。
- 优化自定义手势动作选择区域，减少拥挤和误点。
- 优化手势绑定页滚动与右侧动作编辑器常显体验。
- 增强自定义手势识别稳定性。
- 默认补充 R+L：右键按住 + 左键点击组合手势。
- 增加“导出诊断包”，便于 Beta 用户反馈问题。
- 补充 README、HELP、UPDATE、BETA_TEST、KNOWN_ISSUES 文档。
- 新增 GitHub Bug / 功能建议 Issue 模板。

### 修复

- 修复手势绑定页左侧列表滚轮体验异常的问题。
- 修复右侧动作编辑器被滚走后出现大面积空白的问题。
- 修复自定义手势入口按钮点击无效的问题。
- 修复自定义手势添加后不立即显示的问题。
- 修复设置页自定义手势识别阈值过高导致识别失败的问题。

### 已知问题

- 当前 Beta 包未签名，Windows SmartScreen 可能提示未知发布者。
- 管理员权限窗口中，普通权限运行的 GestureClip 可能无法稳定执行输入模拟。
- 图片历史数量很大时，首次缩略图加载仍可能需要一点时间。
- 覆盖更新前必须先退出程序，否则 `GestureClip.exe` 可能被占用。

## GestureClip v0.5.0 Beta

### 重点更新

- 优化剪贴板快速点击复制的流畅度。
- 优化大量历史记录下的列表加载速度。
- 优化搜索响应速度。
- 优化图片缩略图加载策略。
- 优化数据库写入和统计更新，减少 UI 卡顿。
- 优化剪贴板图片预览与重新复制。
- 优化手势绑定页面，默认突出常用手势。
- 强化自定义手势入口。
- 优化手势触发方式设置。
- 优化边缘滑动触发体验。
- 优化工位小熊设置页。
- 新增过劳提醒 / 猝死提醒。
- 动作 HUD 支持根据工作时间阶段变色。
- 优化动作 HUD 可读性。
- 优化退出流程，减少后台残留风险。
- 增强覆盖更新兼容性。

### 修复

- 修复快速复制时可能卡顿的问题。
- 修复部分设置文字显示不完整的问题。
- 修复手势设置页部分交互不明显的问题。
- 修复剪贴板图片预览不清晰的问题。
- 修复部分场景退出后可能残留进程的问题。
- 修复 HUD 浅色背景下文字看不清的问题。

### 已知问题

- 当前 Beta 包未签名，Windows SmartScreen 可能提示未知发布者。
- 管理员权限窗口中，普通权限运行的 GestureClip 可能无法稳定执行输入模拟。
- 图片历史数量很大时，首次缩略图加载仍可能需要一点时间。
- 覆盖更新前必须先退出程序，否则 `GestureClip.exe` 可能被占用。
