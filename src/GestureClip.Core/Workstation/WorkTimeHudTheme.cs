namespace GestureClip.Core.Workstation;

public sealed record WorkTimeHudTheme(
    string Key,
    string StartColor,
    string EndColor,
    string AccentColor,
    string ShortStatusText,
    string FriendlyColorName);
